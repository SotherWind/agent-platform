# 任务

- [x] 规格四件套
- [x] 脚手架 `web/bi-analyst`（Vite + React + TS），workspace 加入 `web/*`
- [x] model：types / session / pipeline / threads / examples
- [x] `parse-sse` + `BiAnalyzeChatTransport`
- [x] `useAskDataChat` + `useBackendHealth`
- [x] 展示组件（SessionRail 到 Composer，含 ErrorNote）
- [x] `AskDataPage` 组装
- [x] token / 全局样式，对齐 concept.png
- [x] 根 README 与本包 README
- [x] 浏览器验收：空态、健康检查、发问、pipeline、停止按钮、新会话、线程切换、错误展示
- [ ] 图表 / 澄清：当前后端 LLM `fetch failed`，需模型可达后再验
